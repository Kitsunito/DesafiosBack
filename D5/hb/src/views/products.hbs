{{#if productos}}
    <div>
        <table align="center">
            <thead>
                <tr>
                    <th>Título</th>
                    <th>Precio</th>
                    <th>Imagen</th>
                </tr>
            </thead>
    {{#each productos}}
        <tr> 
            <td>{{this.name}}</td>
            <td>$ {{this.price}}</td>
            <td><img alt="item img" src="{{this.thumbnail}}" width="150px" height="150px"/></td>
        </tr>
    {{/each}}
    </table>
    </div>
    {{else}}
    <p>No hay productos cargados</p>
{{/if}}